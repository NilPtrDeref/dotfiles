-- Keep only your personal keybinding overrides here. Add new bindings or
-- unbind defaults before replacing them.

-- See current bindings and descriptions:
--   omarchy menu keybindings --print

-- To disable every Omarchy default binding, set this in
-- ~/.config/hypr/hyprland.lua before require("default.hypr.omarchy"), then add
-- only the bindings you want below:
--   omarchy_default_bindings = false

-- To disable all preinstalled app/webapp bindings, set:
--   omarchy_preinstalled_bindings = false

-- Add a new binding.
-- o.bind("SUPER + SHIFT + R", "SSH", "alacritty -e ssh your-server")

-- Change an existing binding by unbinding it first, then binding the key again.
-- This example changes SUPER+SPACE from the launcher to the Omarchy root menu.
-- hl.unbind("SUPER + SPACE")
-- o.bind("SUPER + SPACE", "Omarchy menu", "omarchy-menu toggle root")

-- Disable a default binding without replacing it.
-- hl.unbind("SUPER + SHIFT + B")

-- Logitech MX Keys examples:
-- o.bind("SUPER + SHIFT + S", nil, "omarchy-capture-screenshot")
-- o.bind("SUPER + H", nil, "voxtype record toggle")
-- o.bind("SUPER + PERIOD", nil, "omarchy-shell shell toggle omarchy.emojis")

-- Terminal Override
hl.unbind("SUPER + SHIFT + T")
o.bind("SUPER + SHIFT + T", "Terminal", "omarchy-launch-terminal")

-- Menu Override
hl.unbind("SUPER + SPACE")
hl.unbind("SUPER + ALT + SPACE")
o.bind("ALT + SPACE", "Launch apps", "omarchy-menu toggle app")
o.bind("ALT + SHIFT + SPACE", "Omarchy menu", "omarchy-menu")

-- Fix mouse window drag bindings
hl.unbind("SUPER + mouse:272")
hl.unbind("SUPER + mouse:273")
hl.bind("ALT + mouse:272", hl.dsp.window.drag())
hl.bind("ALT + mouse:273", hl.dsp.window.resize())

-- Window movement
hl.bind("ALT + H", hl.dsp.focus({ direction = "left" }))
hl.bind("ALT + J", hl.dsp.focus({ direction = "down" }))
hl.bind("ALT + K", hl.dsp.focus({ direction = "up" }))
hl.bind("ALT + L", hl.dsp.focus({ direction = "right" }))

-- Window repositioning
hl.bind("SHIFT + ALT + H", hl.dsp.window.swap({ direction = "left" }))
hl.bind("SHIFT + ALT + J", hl.dsp.window.swap({ direction = "down" }))
hl.bind("SHIFT + ALT + K", hl.dsp.window.swap({ direction = "up" }))
hl.bind("SHIFT + ALT + L", hl.dsp.window.swap({ direction = "right" }))

-- Screenshot
hl.unbind("SUPER + SHIFT + S")
o.bind("SUPER + SHIFT + S", "Screenshot region", "omarchy-capture-screenshot region")
